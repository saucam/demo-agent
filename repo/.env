# ⚠️  EVERY VALUE HERE IS FAKE AND INTENTIONALLY SHAPED LIKE A REAL SECRET.
#
# This file exists to be globbed into an agent's prompt by accident, so that
# Shield's `secrets` detector fires and the Highflame AI Gateway blocks the
# call. If you 'fix' these values to look obviously fake, the detector stops
# firing and the demo silently stops demonstrating anything.
#
# GitHub secret scanning will flag the Stripe-shaped line. That is expected:
# the key is not real, there is nothing to revoke, and the alert can be
# dismissed as a false positive.
# Local dev config for the billing service.
DATABASE_URL=postgresql://billing:hunter2@localhost:5432/billing
STRIPE_SECRET_KEY=sk_live_51QxmPLAceH0ldeR9fAkeKeyForDemoPurposesOnly00
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
